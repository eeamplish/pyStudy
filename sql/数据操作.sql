-- show tables;
-- 使用哪个数据库
-- use test11;

-- 在数据库中创建表 
-- create table student1(
-- 	id int,
-- 	name varchar(10),
-- 	age int
-- );

-- 插入数据
-- insert into student(id, name, age) values(1, 'one', 13), (2, 'two', 56), (3, 'three', 24);
-- insert into student(id, name) values(4, 'xxx'), (5, 'five');
-- drop table student;

-- insert into student(id, name, age) values(7, 'seven', 13), (9, 'nine', 56), (8, 'eight', 24);

-- 删除数据
-- delete from student where id%2=0;
-- 数据库数据全部删除 
-- delete from student;

-- 更新数据
-- update student set name = 'first' where id = 1;
-- 不写where就是修改全部数据
-- update student set age = 666;

-- 查询数据
-- select * from student where id > 5;
